environment:
  # Provides Azure OIDC credentials (via azure-base) and hub VNet references for spoke consumers
  - azure-data/hub-network
config:
  # Stack-specific inputs consumed by __main__.py — not provided by ESC
  azure-data-hub-network:location: westeurope
  azure-data-hub-network:addressSpace: "10.0.0.0/16"
  azure-data-hub-network:environment: dev
